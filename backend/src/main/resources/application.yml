spring:
  application:
    name: backend

  profiles:
    active: dev

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

server:
  port: 8080

springdoc:
  paths-to-match: /api/**
